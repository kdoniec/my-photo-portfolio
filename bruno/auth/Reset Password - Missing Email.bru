meta {
  name: Reset Password - Missing Email
  type: http
  seq: 7
}

post {
  url: {{baseUrl}}/api/auth/reset-password
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
  }
}

assert {
  res.status: eq 400
  res.body.error: eq "Email jest wymagany"
}

docs {
  # Reset Password - Missing Email

  Test braku email w żądaniu resetowania hasła.

  ## Oczekiwana odpowiedź (400)
  ```json
  {
    "error": "Email jest wymagany"
  }
  ```
}
