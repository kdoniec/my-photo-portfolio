meta {
  name: Login - Missing Email
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/api/auth/login
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "password": "somepassword"
  }
}

assert {
  res.status: eq 400
  res.body.error: eq "Email i hasło są wymagane"
}

docs {
  # Login - Missing Email

  Test braku email w żądaniu logowania.

  ## Oczekiwana odpowiedź (400)
  ```json
  {
    "error": "Email i hasło są wymagane"
  }
  ```
}
